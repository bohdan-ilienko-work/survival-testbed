# Survival testbed — як тестувати

Усе вже підняте. Просто відкрий **http://localhost:5500**

## Порядок кліків

1. **Підключити сервери** — gateway відкриє JSTP-з'єднання до main / fight / survival
2. **Мок-юзер** — `auth.signUp` + `auth.signIn` на main-server (справжній шлях, не підробка)
3. **+50 🎟** — видати тікети через справжній gRPC `MainServerAPI.GrantTickets`
4. **Хто зареєстрований** — `beG.getSurvivalStatus` на main-server: список записаних
   ще до входу в лоббі (див. наступний розділ)
5. **Зайти в Survival** — `beG.joinSurvival` на main-server → токен → `survival.connect(token)`

Далі все їде саме: лоббі добивається ботами, стартує матч, приходять раунди.
Відповідай — і дивись екран результатів, вибування, BuyBack, фінал.

Щоб просто **подивитись**, чи йде матч, нічого з цього не треба: кнопка
**«👁 Дивитись матч»** працює без акаунта й без тікета — див. «Режим глядача» нижче.

## Відповідь іде сама (autosubmit)

Кнопки «Відповісти» більше не треба — відповідь вирушає на сервер **щойно вона стає
повною**, так само як у Unity-клієнті. Що означає «повна» — залежить від того, з чого
відповідь складається:

| Режим | Коли летить | Ручний шлях |
|---|---|---|
| `QUESTION` | клік по варіанту — сам клік і **є** відповіддю | той самий клік |
| `MAP` | **мітка поставлена**. Пересунути її вже не можна | клік по карті |
| `NUMBER` | значення **підтверджене**: `Enter` або втрата фокуса полем | кнопка «Відповісти» |
| `CHRONO` | **кожна з трьох** подій отримала рік | кнопка «Відповісти — N з 3» |

Три речі, які варто перевірити саме тут:

1. **Рівно одна відповідь на раунд.** Клацай далі після того, як відповідь пішла: панель
   гасне (бейдж «відповідь надіслано»), і другого `survival.submitAnswer` у логу не
   зʼявляється. Це замок, привʼязаний до **ключа питання**, а не прапорець, — тож раунд,
   що завершився поки запит ще летів, нічого не «заклинює»: наступне питання просто має
   інший ключ.
2. **NUMBER не летить по літері.** Набери `1`, зачекай, добери `234` — має піти `1234`,
   а не `1`. Навмисно **немає** дебаунсу по набору: пауза між цифрами не повинна коштувати
   раунду. Підтвердження — `Enter` або клік повз поле.
3. **CHRONO більше не можна переграти.** Щойно рік отримує **третя** подія, відповідь іде.
   Тобто **міняти пари місцями можна лише поки хоч одна подія порожня** — це справжня
   ціна autosubmit, і на трьох подіях вікно для правок стало вужчим, тож саме її треба
   поганяти. Кнопка внизу лишилась для іншого шляху: свідомо **неповної** пари (сервер
   приймає `-1`), у якої моменту завершення просто немає.

## CHRONO — подій рівно три

Довжина CHRONO зафіксована на сервері: **3 події і 3 роки**, з будь-якого джерела питань —
денного набору, легасі-бази chrono_db чи пулу з questions-api. Це константа
(`CHRONO_EVENTS_PER_ROUND` у `survival-server/src/common.ts`), а не змінна оточення:
`SURVIVAL_CHRONO_EVENTS_PER_ROUND` більше не існує — її прибрано і з коду, і з compose-файлів,
і з `.env.testbed`.

**Як загнати себе в CHRONO-раунд.** Режим кожного раунду задає слот денного набору, тож
найкоротший шлях — набір з одних лише CHRONO:

```bash
node seed/make-survival-set.js --formats CHRONO --rounds 6 --state LOCKED --start-in-seconds 60
```

Один формат — легально: правило «не повторювати формат на межі циклу» вимикається, коли
формат один, тож кожен раунд приїде CHRONO. Потрібне чергування — `--formats CHRONO,CHOICE`.
Без набору режими роздає запасний цикл, і CHRONO там доводиться просто чекати.

**Що має бути на екрані:**

- рівно **три** рядки-події, у кожному — смужка рівно з **трьох** фішок років;
- «вільні роки» на старті теж три, і список порожніє з кожною парою;
- кнопка «Відповісти — 0 з 3», і число зліва росте з кожною парою. На третій парі
  вона встигає показати «3 з 3», але вже **мертвою**: відповідь пішла в тому ж кліку,
  панель згасла, зверху бейдж «відповідь надіслано», фішки й кнопка вимкнені;
- у «Стані клієнта» рядок «моя відповідь» після autosubmit показує `pairs` рівно
  з **трьох** чисел — це та сама довжина, якої сервер чекає від `events`.

**Обрізана заготовка — окремий випадок, який варто перевірити.** Денний набір міг бути
зверстаний на 4+ подій (`seed/make-survival-set.js` бере рівні на 3..8 фактів). Сервер ріже
таку заготовку до **перших трьох подій із придатним, неповторним роком** — детерміновано,
той самий набір дає ту саму трійку на кожному прогоні. Перевірка: візьми слот із
`survival_factory.survival_sets`, у якого в `payloadSnapshot` більше трьох подій, і звір
його з тим, що приїхало в `roundStarted` — на екрані мають бути перші три, що пройшли
фільтр року, а не перші три зі снапшота.

**Роки перемішані чесно — усі шість перестановок рівноймовірні.** Тотожну сервер навмисно
не викидає: прибрати її означало б лишити п'ять варіантів замість шести, і гравець, який
просто ніколи не ставить перший рік першій події, вигравав би раунд із п'яти замість одного
з шести. Тож «спаруй згори вниз» — звичайна відповідь, правильна приблизно в одному раунді
з шести. Якщо вона не зарахувалась жодного разу за пару десятків раундів — це вже підозріло.

**Неповна пара нікуди не поділась.** Спаруй лише дві події й тисни кнопку: піде `pairs`
з одним `-1`, і сервер таку відповідь приймає. На трьох подіях це ще й єдиний спосіб
побачити бал **67** — повна пара дає тільки 0, 33 або 100, бо одна переставлена пара
завжди коштує двох рядків.

## Тайбрейк — раунд, який вирішує додаткове питання

Тайбрейк — **не раунд**: він не займає номера раунду, не шле `roundStarted` і не
рахується в ліміт раундів. Раніше це виглядало як питання, що взялося нізвідки. Тепер
у смужці кроку зʼявляється червоний бейдж — **«ТАЙБРЕЙК · усі помилились · 2/5»** — і
він же є у глядача. Наведи на бейдж: у підказці — **хто саме** грає тайбрейк.

Три фази, кожна зі своїм виглядом:

- **`tiebreakPending`** — пауза показу: раунд зіграно, всі помилились, питання ще не
  відкрите. Бейдж пульсує й рахує «питання через N с». Внизу — таблиця **того раунду,
  що завалився** (усі ❌), над нею — пояснення. Ніякого «наступний раунд через…»: його
  й не буде, поки тайбрейк не скінчиться.
- **`tiebreakStarted`** — додаткове питання відкрите. Режим береться **з події**
  (`MAP` малюється картою, а не полем для числа — раніше було зашито `NUMBER`, і MAP-тайбрейк
  неможливо було відповісти). Хто **не** в списку `playerIds`, бачить панель вимкненою з
  бейджем «глядач» — сервер їхню відповідь усе одно не візьме.
- **`tiebreakResult` → `roundResult`** — бейдж сірішає («зіграно») і **лишається** на
  екрані результатів: саме він пояснює вибування, якого жодна колонка таблиці не пояснює.
  Режим у таблиці повертається до режиму **раунду** (не тайбрейку), тож `QUESTION`-раунд
  більше не малюється з колонкою «похибка».

Причина приходить полем `reason` / `roundResult.tiebreakReason`: `all_wrong` — «усі
помилились», `boundary_tie` — «нічия на межі». Старіший сервер шле лише
`tiebreakPlayerIds` — тоді бейдж є, а причина чесно каже «сервер не назвав».

Швидкий спосіб побачити все це: `SURVIVAL_BOT_ACCURACY=0` — кожен `QUESTION`-раунд
стає all-wrong. На CHRONO-наборі (див. розділ вище) тайбрейк приходить ще певніше:
ботам він дістається за тією ж накруткою точності, а скриптовий прогін `e2e/run-match.js`
завалює CHRONO **завжди** — див. нижче. У рядку «тайбрейк» у «Стан клієнта» видно сирі фазу / причину /
ітерацію / розмір когорти.

## Кілька турнірів на день і байбек за 1 тікет

День більше не дорівнює одному турніру. В адмінці фабрики (**Налаштування**) поле
«Старти турнірів (UTC)» — список часів; **скільки записів, стільки турнірів на день**.
Набір лишається один на дату: слотів генерується `роундів × кількість турнірів`, і
кожен турнір грає **своє вікно** слотів — два турніри одного дня ніколи не покажуть
одне питання двічі. У модалці часу конкретного дня можна додати/прибрати турнір
(зміна кількості — тільки для DRAFT: слоти перегенеруються під нову кількість вікон;
для LOCKED можна лише пересувати вже наявні старти).

Що видно в тестбеді: у попапі «Хто зареєстрований» поруч зі станом зʼявляється
**«турнір: 2 з 3»** — але тільки коли день справді багатотурнірний, «1 з 1» не
пишеться. Після фіналу першого турніру нове лобі саме відкривається на наступний
старт дня.

**Байбек тепер коштує 1 тікет** — кожна спроба, будь-який раунд (стара крива
`2,4,10,15,25` × множники раундів вимкнена дефолтами; env-важелі
`SURVIVAL_BUYBACK_FIXED_COSTS` / `SURVIVAL_BUYBACK_ROUND_MULTIPLIERS` живі, якщо
захочеться повернути). А `buyBackSuccess` тепер несе **імʼя** гравця — на екрані
результатів зʼявляється зелений рядок «↩ такий-то викупився назад у гру», і в
глядача теж: без нього лічильник живих просто тихо ріс на одиницю.

Пауза показу відповідей перед тайбрейком — **3 секунди**
(`SURVIVAL_TIEBREAK_REVEAL_MS`, було 4000). Пуш «Survival почнеться за 5 хвилин»
шле main-server сам (крон раз на хвилину, вимикається `SURVIVAL_START_PUSH=off`;
у development і під `SURVIVAL_INSTANT_START` не спрацьовує — там старт завжди «зараз»).

## Екран реєстрації — хто записався ще до лоббі

Кнопка **«Хто зареєстрований»** — це один виклик `beG.getSurvivalStatus` на
main-server, той самий, яким головне меню малює попап бронювання за години до
матчу. Токен на `survival.connect` живе 10 хв, тож ростер із JSTP-сесії для
такого екрана не годиться — і саме тому кнопка **не** потребує з'єднання з
survival-server: тисни її одразу після «Мок-юзер», ще до «Зайти в Survival».

У панелі **«Реєстрація на матч»** має бути:

- **N гравців зареєстровано** — це `lobby.playerCount` із відповіді, не довжина списку
- **стан** лоббі і **старт** (`scheduledStartAt`) зі зворотним відліком. До першого
  входу це `BOOKING`; у цьому стенді `SURVIVAL_INSTANT_START=true`, тож щойно
  зайшов перший гравець — стан перескакує в `ONBOARDING`, а реєстрація лишається
  відкритою (у проді вона на цьому закривається)
- **✔ / ✖ ти зареєстрований** — прапорець `registered`
- список учасників: **№ · прапор · персонаж · нікнейм · клан**. Номер береться з
  позиції в масиві (`slot` до старту бою — `null`), порядок реєстраційний і не
  сортується — свій рядок підсвічено
- **«Оновити список»** — повторний виклик: подій на цьому шляху немає, панель не жива

### Прапор і персонаж — справжня графіка з гри

Рядок ростера має виглядати як у грі, а не як емодзі-заглушка:

- **прапор** — картинка країни з `flagcdn.com` (куплений преміум-прапор — з wikimedia).
  Картинки **віддалені**, тобто браузеру потрібна мережа; адмінка, звідки взято цей
  мапінг, робить так само
- **персонаж** — портрет із `web/public/characters/` (локальні файли, скопійовані з
  `../admin-tools-react-frontend/public/characters/`)
- **нікнейм** — `user<playerId>`, який main-server видає свіжому акаунту
- **клан** — назва клану, порожньо поки гравець без клану

Уся відповідність «індекс → портрет» і «код або назва → прапор» лежить в одному
файлі — `web/src/gameAssets.ts`. Бита чи не завантажена картинка рядок не ламає:
у кожної є `onError`.

**Щоб побачити різних персонажів.** Gateway роздає персонажів по колу — усі 29, по
одному на **створений акаунт** (не на вкладку):

- 3-4 вкладки → 3-4 різні портрети
- **«Новий гравець»** у тій самій вкладці → теж наступний персонаж

Ротація починається з `1` і до `0` (Darwin) доходить останньою: саме `0` survival-server
дає **всім ботам**, тож у маленькому лоббі живі рядки видно з першого погляду.

**Щоб побачити різні прапори.** Усі з'єднання в стенді йдуть з localhost, geoip нічого
не знаходить — і main-server ставить кожному `'UN'`, тобто за замовчуванням прапор в
усіх живих гравців однаковий. Кнопка **«Змінити прапор»** — це справжній
`beG.changeFlag` на main-server, з його ж перевірками:

- невідома назва → `Wrong flag name`. Звіряння йде з `flags.all`, який
  `init/loadJSONs.js` збирає як `free.concat(premium)`: 245 кодів країн із
  `lib/flags.js` (там же `UN` і `??`) плюс 7 преміум-назв — разом 252
- преміум-прапор, не куплений цим гравцем → `not bought`: назва проходить
  перевірку вище й падає на наступному рядку, бо `boughtFlags` у свіжого акаунта
  порожній. Купити його можна через `shop/buyFlag` (250–500 гемів, а в
  DEVELOPMENT_BUILD гемів видають 100000), але зі стенду це недосяжно з іншої
  причини: gateway інспектує лише інтерфейси `beG`, `auth`, `adminApi` — `shop`
  серед них немає. Другий шлях — `adminApi.grantToPlayer` з `flags: [...]`
- той самий прапор, що вже стоїть → `Already selected`

**Прапор і клан міняй ДО «Зайти в Survival».** main-server бере `player.flag`,
`player.character` і назву клану в момент `RegisterPlayer`, а survival-server,
побачивши вже зареєстрованого гравця, реєстрацію **пропускає** (`alreadyRegistered`
у `grpcServer.ts`) — видає новий токен, але збережений профіль у ростері не
оновлює. Змінене після входу з'явиться аж у наступному лоббі.

**У ботів немає ні прапора, ні клану — так і задумано.** `botProfile` у
survival-server віддає `flag: ''` і `clan: ''`, тож ці дві колонки в ботських рядках
лишаються **порожніми** (порожньо, а не заглушка), а портрет у всіх ботів один —
Darwin. Це не баг і не проґавлений фолбек: у живого гравця прапор порожнім не буває
ніколи, бо main-server ставить `'UN'`, коли geoip не визначив IP.

### Щоб колонка «клан» не була порожньою

Свіжий мок-акаунт клану не має, тому в ростері приїде `''`. Кнопка **«Створити клан»**
у тій самій панелі — суто тестова (`adminApi.clanAdmin` на main-server, без авторизації
й без вимог до рівня): вона кладе гравця цієї вкладки в клан **Testbed Clan**.

Тиснути її треба **до «Зайти в Survival»**: назву клану main-server підставляє в
`RegisterPlayer`, тобто клан фіксується в момент реєстрації — створений пізніше
з'явиться аж у наступному лоббі.

Два гравці з однаковим кланом:

1. вкладка №1: **Мок-юзер** → **Створити клан** → **Зайти в Survival**
2. **нова вкладка**: **Створити клан** → **Зайти в Survival** — клан один на процес
   gateway, тож друга вкладка додається в той самий (`created: false` у відповіді)
3. у будь-якій із них: **Хто зареєстрований** → у списку два рядки з іменем
   `Testbed Clan`. Боти клану не мають ніколи — і в списку вони сидять з самого
   відкриття лоббі: сервер сіє їх одразу (`playerJoined` приходить і для
   `BOT_...`), а онбординговий таймер лише добирає ними вільні місця, що
   лишились. Тому кожен лічильник у клієнті показує розбивку
   «X гравців + Y ботів» — голе число було б переліком ботів

## Режим глядача — подивитись матч, нікуди не заходячи

Кнопка **«👁 Дивитись матч»** у групі **«Глядач»**. Це єдина кнопка в стенді, якій
**не потрібен ані гравець, ані тікет, ані `beG.joinSurvival`**: gateway відкриває
з'єднання з survival-server і одразу кличе `survival.spectate`. Тиснути її можна
першою — до «Мок-юзер», до «+50 🎟», взагалі без акаунта. Саме ця відсутність
воріт і є фічею: раніше єдиний спосіб дізнатись, чи йде зараз матч, — читати логи
сервера.

Що видно на екрані глядача:

- **шапка** — крок, раунд, режим раунду, скільки ще глядачів (`viewers`) і зворотний
  відлік до дедлайну відповіді
- **картка лоббі** — стан (`BOOKING` / `ONBOARDING` / `ACTIVE`), стан бою
  (`ROUND_ACTIVE`, `BUYBACK_WINDOW`, `INTERMISSION`…), «N у грі з M» з розбивкою
  «X гравців + Y ботів» — і окремо стільки ж по всьому ростеру
- **ростер** — той самий список, що й на екрані реєстрації (номер, прапор, персонаж,
  нікнейм, клан), вибулі рядки притемнені
- **питання** — та сама панель, що й у гравця, але вимкнена (бейдж «глядач»);
  під нею — **«уже відповіли»**: імена тих, хто вже здав відповідь
- **результати раунду** — та сама таблиця, що бачить гравець: відповіді, похибка,
  поріг раунду, хто вибув, зворотний відлік до наступного раунду
- **фінал** — та сама підсумкова таблиця з нагородами, тільки без кнопки «Зіграти ще»
  (глядачеві нема звідки грати ще)

### Що саме тут перевіряти

1. **Вхід посеред бою.** Відкрий вкладку, коли матч уже йде, і тисни «Дивитись матч».
   Спершу приїде знімок (`spectate`) — питання, дедлайн, хто вже відповів, ростер із
   вибулими, — і лише потім екран живе на подіях. Якщо замість цього видно порожній
   лоббі — знімок не застосувався.
2. **Ротація лоббі.** Не закриваючи вкладку, дочекайся кінця матчу. Сервер сам
   перенаправляє глядача на наступне лоббі подією `spectatorLobbyChanged` — екран
   має перемалюватись на нове `BOOKING` лоббі, а таблиця щойно завершеного матчу
   лишається під ним у рамці. **Ніяких кліків для цього не треба.**
3. **Коли нічого не йде.** Якщо сервер відповів `lobby: null`, екран каже
   «Матч не заплановано» і сам перепитує `spectate` кожні 5 с (не частіше — інакше
   сервер відповість `too_fast`). Якщо ж лоббі є, але в стані `BOOKING`, видно
   **час наступного старту** і «через N хв».
4. **Приватного не приходить.** У списку «Події сервера» праворуч у глядача **не має
   бути** `buyBackOffer`, `buyBackDenied`, `ticketsUpdated`, `playerReconnected` —
   це приватні події, адресовані конкретному гравцеві. Публічні (`roundStarted`,
   `roundResult`, `playerEliminated`, `buybackWindowOpen`…) — навпаки, мають бути всі.
5. **Правильної відповіді в живому раунді немає.** Це видно просто в «Події сервера»:
   `roundStarted` приходить **без** `correctAnswer`, і лише `roundResult` — уже після
   закриття раунду — несе її. Тобто рівно тоді ж, коли її бачать усі гравці. Так само
   і зі знімком: `spectate` посеред відкритого раунду віддає питання без відповіді.
6. **Дві ролі не змішуються.** У вкладці, яка вже зайшла в матч, `spectate` відмовить
   (`already_in_match`) — це навмисно: інакше один оплачений тікет перетворився б на
   безкоштовну трансляцію всіх наступних матчів.

**«↻ Оновити кадр»** — та сама кнопка, коли місце вже зайняте: `spectate`
ідемпотентний, повторний виклик віддає свіжий знімок на тому самому місці й другого
слота не займає. Це ж і шлях відновлення після обриву з'єднання (сокет новий — місце
треба зайняти заново). **«Досить дивитись»** — `stopSpectating`, звільняє місце.

Годинник екрана — **серверний**: знімок несе `serverNow`, і всі зворотні відліки
рахуються з поправкою на розбіжність (її видно внизу екрана, «розсинхрон годинника»).
Усі моменти на дроті — абсолютні epoch ms, ніколи не тривалості.

### Ліміти й вимикач (env survival-server)

| Змінна | Дефолт | Що робить |
|---|---|---|
| `SURVIVAL_SPECTATORS_ENABLED` | `true` | вимикач на час інциденту; `false` → `spectator_disabled` |
| `SURVIVAL_MAX_SPECTATORS` | `200` | стеля місць; понад неї → `spectator_capacity`, без витіснення |
| `SURVIVAL_SPECTATE_MIN_INTERVAL_MS` | `1000` | мінімальний інтервал між знімками; частіше → `too_fast` |

Стеля рахує **лише глядачів** — навала глядачів не може коштувати місця жодному
платному гравцеві. **Одна вкладка = одне місце**: gateway тримає окреме з'єднання з
survival-server на кожну вкладку браузера.

## Кілька гравців — просто відкрий кілька вкладок

Кожна вкладка = **окремий гравець**: свій акаунт, свої JSTP-з'єднання, свої тікети.
Це не косметика — особистість у грі живе на з'єднанні (`connection.player` на
main-server, прив'язка сокета в `survival.connect`), тож спільне з'єднання
зробило б із вкладок одного гравця.

- **нова вкладка** → новий гравець автоматично
- **перезавантаження (F5)** → той самий гравець (акаунт лежить у `sessionStorage` вкладки),
  тож персонаж і прапор теж лишаються ті самі
- **кнопка «Новий гравець»** → створити свіжого прямо в цій вкладці

Відкрий 3-4 вкладки, у кожній: Мок-юзер → +50 🎟 → Зайти в Survival — і всі
потраплять в одне лоббі, кожен зі своїм персонажем. У шапці видно номер вкладки
й playerId, щоб не плутатись.

### Що читати на лічильниках

Скрізь, де показано, **скільки лишилось у грі**, тепер стоять слова, а не голе число:
«залишилось 5 гравців», «залишився 1 гравець» — у колонці ростера праворуч, у картці
лоббі глядача і в «Стан клієнта». Пара цифр `5/12` біля списку читалась як рахунок або
номер сторінки; у виживанні жива саме перша.

### Перезахід у наступне лоббі — раунду бути не повинно

Зіграй матч до кінця й **тією самою вкладкою** тисни «Зайти в Survival» ще раз. У новому
лоббі, поки воно чекає на гравців, у смужці кроку **не має бути ані «раунд N», ані
пігулки режиму**, а «Стан клієнта» має показувати `раунд / режим: 0 · —`, `вибув: ні`,
`нагороди: не знаю`. Раніше вкладка тягла це все з попереднього матчу — і, що гірше,
`iAmEliminated` теж, тож перший же `roundStarted` нового матчу кидав гравця в режим
глядача. Той самий скид тепер робить і `fightStarted`, і привʼязка сокета до **іншого**
lobbyId.

## Що де

| Що | Адреса |
|---|---|
| Веб-застосунок | http://localhost:5500 |
| Gateway (JSTP↔WebSocket) | ws://localhost:8787 |
| main-server (JSTP) | 127.0.0.1:7000 |
| fight-server (JSTP) | 127.0.0.1:7777 |
| survival-server (JSTP) | 127.0.0.1:4001 |
| survival-server (gRPC) | 127.0.0.1:5010 |
| main-server (gRPC, тікети) | 127.0.0.1:5012 |
| questions-api | http://localhost:3001 |
| mongo | mongodb://127.0.0.1:27077 |

## Прискорені налаштування (щоб не чекати 17:00 UTC)

У `.env.testbed`: `SURVIVAL_INSTANT_START=true`, мінімум 4 гравці,
онбординг 15 с, раунд 20 с, вікно BuyBack 10 с. У проді дефолти лишились бойові.

## Автоматичний прогін матчу (без браузера)

```bash
node e2e/run-match.js --players 3 --accuracy 0.7
```

Створює N гравців, видає тікети, заводить у лоббі й відповідає за них.
Виходить з кодом 0 лише якщо матч **дійшов до фіналу** — тобто ловить
класичне «бій завис назавжди».

Дві речі про CHRONO у цьому прогоні:

- **скрипт відповідає і на тайбрейк.** Тайбрейк не шле `roundStarted`, тому раніше
  вирішальне питання просто нікому було відповідати: когорта мовчала, декілька ітерацій
  згорали в таймаути. Тепер є обробник `tiebreakStarted` — режим береться з події
  (`MAP` або `NUMBER`, ніколи не режим самого раунду), відповідає лише названа
  когорта. У підсумку зʼявився рядок `tiebreaks : N · {"NUMBER":N}`;
- **CHRONO скрипт завалює завжди, і це не баг.** Гармата не бачить правильної пари, а
  єдина пара, яку вона могла б вгадати «за замовчуванням» — згори вниз — саме та, яку
  сервер зробив неможливою. Тож `--accuracy` на CHRONO нічого не змінює: раунд усе одно
  йде в all-wrong-тайбрейк. Гілка «неправильно» надсилає одну подію як `-1`, щоб бути
  неправильною **напевно**: раніше вона слала ротацію, яка на трьох подіях виявлялась
  правильною в кожному шостому прогоні, і `--accuracy 0` переставав бути надійною
  фікстурою all-wrong.

## Керування

```bash
./up.sh          # підняти стек
./down.sh        # зупинити
node seed/index.js   # перезасіяти питання (517 шт.)

# gateway і web (якщо треба перезапустити)
cd gateway && node index.js
cd web && npm run dev
```

## Важливо

База **локальна й ізольована** (`mongodb://127.0.0.1:27077`). Стенд ніколи не
ходить у бойову базу — `up.sh` навіть відмовиться стартувати, якщо в env
знайде віддалені адреси.
